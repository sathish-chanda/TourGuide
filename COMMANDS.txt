We built this project using spring initilizer with the following dependencies. 
a. Spring Web
b. Rest Repositories
c. Spring Data JPA 
d. H2 Database
e. Lombok

### BUILD and RUN commands ####

1. cd explorecali-jpa 
2. mvn clean install ---> to build the project
3. java -jar target/explorecali-jpa-0.0.1-SNAPSHOT.jar   ---> to run the project
4. curl localhost:8080 ---> to test if the project is running.

### Build and run the using the image

1. Add the mvn jib dependencies
2. mvn jib:dockerBuild [take our application turn into an image]
3. docker images
4. docker run -p 8080:8080 explorecali-jpa:0.0.1-SNAPSHOT
5. open localhost:8080
6. docker stop containerID

### Storing data in persistant storage :: mysql
1. Update the pom.xml file to have the dependencies spring-boot-docker-compose [This helps to connect containers], mysql-connector-j
2. Create file compose.yml which will have the configurations of the mysql database.
3. $ mvn install spring-boot:run
4. The above command will download and launch the mysql in the background which will be used by the application.
5. $ docker exec -it mysqlcontainerID mysql -u [root/myuser] -p 
6. The above command used to connect to mysql database
7. $ SHOW DATABASES;
8. $ SHOW TABLES;
9. $ select * from tour;


### Flywaydb plugin
1. It maintains the versioning of the tables.
2. Save the tables only onces and upon restarting the application data will not be added again.
3. $ mvn install spring-boot:run

## After using spring security gateway
0. Mongodb used for storing the images and mysql is used for storing the tour related tables.
1. RUN explorecali-jpa on port 8082. $cd explorecali-jpa $ SERVER_PORT=8082 mvn install -DskipTests 
spring-boot:run
2. Verify the new port is working using the curl command. $ curl https://localhost:8082/tours
3. Start the gateway on port 8080. $ cd explorecali-gateway $ mvn install spring-boot:run
4. Verify if get tours is working form port 8080. $ curl https://localhost:8080/tours $ curl https://localhost:8080/packages
5. $ curl 'http://localhost:8080/packages' -H 'Content-Type: application/json' -d '{"code":"BB", "name":"BeeKeeper"}'
6. Start the explorecali-images microservice on the port number 8081. $ cd explorecali-images $ SERVER_PORT=8081 mvn install spring-boot:run


# SecurityConfig 

Acts as security for our application. If we remove permitAll for GET requests then we have to pass the password when requesting for the localhost:8080/packages. Otherwise it would be unautherized and result would be 401 Unautherized.
$ curl -v -X 'GET' 'http://localhost:8080/packages' -H 'accept: */*' -H 'Content-Type: application/json' -u "user:password"

Where the password will be generated when we run the explorecali-gateway
$ mvn clean install spring-boot:run -> which generates the password
like this line: Using generated security password: b4695e99-3035-4a47-a78b-2de640cb1909
Add this password in the get request to get the results for the localhost:8080/packages.
